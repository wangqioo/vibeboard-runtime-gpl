# Runtime Capabilities

更新时间：2026-07-01

This document separates implemented API, build verification, and board verification. An API is not treated as fully done until it has a real device log in `docs/device-bringup.md`.

## Status Legend

| Status | Meaning |
| --- | --- |
| `board-verified` | Ran on the LCKFB ESP32-S3 board with serial, HTTP, or manual screen evidence. |
| `build-verified` | Compiles in ESP-IDF and has static/package tests, but has not been smoke-tested on board yet. |
| `planned` | Documented target, not implemented yet. |

## Lua Runtime

| Capability | APIs | Status | Evidence |
| --- | --- | --- | --- |
| Lua app execution | `luaL_newstate`, `luaL_openlibs`, `luaL_dofile` | `board-verified` | `apps/smoke_ui` ran from SD. |
| Logging | `print(...)` | `board-verified` | Serial logs show Lua app messages. |
| Timer compatibility | `set_interval(ms, callback)` | `board-verified` | Earlier `smoke_ui` ran with interval callback logs. |
| NodeMCU timers | `tmr.create`, `timer:alarm`, `timer:start`, `timer:stop`, `timer:unregister`, `timer:state`, `timer:interval`, `tmr.now`, `tmr.time` | `board-verified` | `apps/smoke_timer` verified auto timer, single timer, state, unregister, loop idle, and `Lua app ok` on board. |
| Millisecond helper | `millis()` | `board-verified` | Upstream `2048` launched from SD and stayed `state=running,current_app=2048` on 2026-06-19. |
| Runtime version and display brightness | `sys.version`, `sys.getbrightness`, `sys.setbrightness` | `board-verified version + Lua brightness call; physical brightness visual pending` | `lua_sys.c/h` now registers a Lua `sys` module that exposes the committed runtime version string and wraps the board backlight helpers; `npm run test:firmware-static -- --test-name-pattern "sys Lua module"` passes, `idf.py build` passes, and `install_service.c` now shares the version constants through `runtime_version.h`. The board-level backlight helper exists in `board_lckfb_szpi_s3.c`. On 2026-06-25 the `mini_claw` WebUI route smoke called `sys.setbrightness(30)` through POST `/app/api` and returned `{"ok":true,"level":30}` while the app stayed `state=running,current_app=mini_claw`; this verifies the Lua-to-board brightness call path, not a camera/physical observation of screen luminance. |
| File access | `file.exists`, `file.open`, `file.read`, `file.getcontents`, `file.write`, `file.list`, `file.listdir`, `file.stat`, `file.mkdir`, `file.remove`, `file.rename`, `file.rmdir`, `file.putcontents` | `board-verified read/list/open + build-verified mutation helpers` | `apps/smoke_file` verified config read, file handle read, directory listing, app-local write, loop idle, and `Lua app ok` on board. The file module now also exposes app-local stat/mkdir/remove/rename/rmdir/putcontents helpers and structured `file.listdir` entries for service-style apps; that broader surface is currently build-verified and covered by validator/static tests, not fresh board smoke. |
| Key compatibility | `key.on`, `key.off`, `key.push`, `key.repeat_start`, `key.repeat_stop`, `key.LEFT`, `key.RIGHT`, `key.UP`, `key.DOWN`, `key.HOME`, `key.EXIT`, `key.START`, `key.SHORT`, `key.LONG_START`, `key.LONG_REPEAT`, `key.LONG_END` | `board-verified touch + HTTP injection + timer-backed repeat metrics` | Added for upstream `2048`, `nesgame`, and `voice_ai` compatibility; software dispatch and touch-swipe queue bridge are covered by static tests and ESP-IDF build. `2048` double-confirms `HOME`/`EXIT` before app stop, and the user confirmed the physical gesture behavior on 2026-06-19. Physical BOOT short/long press now forwards `HOME` short/long-start into the active Lua app when the Launcher is inactive, so apps using `app.set_home_exit(false)` can handle HOME without immediately returning to Launcher. `key.repeat_start(code[, delay_ms, interval_ms])` and `key.repeat_stop(code)` provide a timer-backed Lua repeat helper that emits LONG_START/LONG_REPEAT/LONG_END through the same queued event path. On 2026-06-22 the narrow board smoke hook `POST /input/key?code=<LEFT|RIGHT|UP|DOWN|HOME|EXIT|START>&event=<SHORT|LONG_START|LONG_REPEAT|LONG_END>` was build/flashed/board-verified: idle injection returns `400 no active app`, `smoke_key` accepted LEFT/HOME injections, and HOME returned it to idle through the normal Lua runner path. On 2026-06-23 `smoke_key` gained machine-readable `metrics.json`, and `npm run input:smoke -- --app smoke_key --key LEFT:SHORT --metrics-polls 14 --require-event UP:LONG_REPEAT --require-event UP:LONG_END` passed on the board with `metrics_count=9`; `/stop` then returned `{"ok":true,"stopped":true}` and `/status` returned idle. Physical BOOT forwarding and hardware-origin long-press repeat still need manual board smoke. |
| Touch-swipe to key bridge | `vb_board_input_start`, `vb_lua_key_enqueue`, `vb_lua_key_process_pending` | `board-verified` | Hardware input task only enqueues events; Lua runner drains pending key events on the Lua/timer loop and cleans up on app stop. User confirmed physical `2048` swipe gameplay on 2026-06-19. |
| Touch coordinates | `touch.on`, `touch.off`, `touch.push`, `touch.DOWN`, `touch.MOVE`, `touch.UP` | `board-verified lifecycle + build-verified physical event path` | Runtime registers a Lua `touch` module and drains board touch down/move/up coordinate events on the same runner input timer as `key`. On 2026-06-22 `smoke_touch` was packaged, uploaded, confirmed in `/apps`, launched through `npm run lifecycle:smoke -- --app smoke_touch`, reached `state=running,current_app=smoke_touch`, then stopped cleanly. This proves the app/API lifecycle path runs on board; physical coordinate label observation still needs manual screen/touch evidence. |
| Key input smoke app | `apps/smoke_key`, `key.on`, `key.push`, `key.repeat_start`, `key.repeat_stop`, `metrics.json` | `board-verified push + repeat metrics + stop cleanup` | `smoke_key` was packaged, uploaded, launched, and left running on board with `current_app=smoke_key,last_status=ESP_OK`; serial logs show alternating `LEFT`/`RIGHT` events from software `key.push()`. On 2026-06-23 the app was upgraded to write `metrics.json` with event counters, to let the timer-backed `UP` repeat run long enough for `LONG_REPEAT`, and to stop its timer when `app.exiting()` is true. The first board pass exposed an old app stuck in `state=stopping`; after reboot and upload of the fixed app, `npm run input:smoke` verified `UP:LONG_REPEAT` and `UP:LONG_END`, and `/stop` returned to idle. Physical touch-swipe label and hardware-origin repeat observation remain pending outside the existing `2048` gameplay evidence. |

## LVGL Lua Surface

| Capability | APIs | Status | Evidence |
| --- | --- | --- | --- |
| Screen and object basics | `lv_scr_act`, `lv_obj_clean`, `lv_obj_create`, `lv_label_create` | `board-verified` | `apps/smoke_ui` displayed weather card. |
| Sizing and alignment | `lv_obj_set_size`, `lv_obj_set_width`, `lv_obj_set_height`, `lv_obj_align` | `board-verified` | `apps/smoke_ui` layout displayed. |
| Basic styles | `lv_obj_set_style_bg_color`, `lv_obj_set_style_text_color`, `lv_obj_set_style_radius`, `lv_obj_set_style_pad_all`, `lv_obj_set_style_border_width`, `lv_obj_set_style_border_color` | `board-verified` | `apps/smoke_ui` card displayed. |
| Constants | `LV_ALIGN_CENTER`, `LV_ALIGN_TOP_LEFT`, `LV_ALIGN_TOP_MID`, `LV_ALIGN_BOTTOM_LEFT` | `board-verified` | `apps/smoke_ui` layout displayed. |
| Positioning and flags | `lv_obj_set_pos`, `lv_obj_set_x`, `lv_obj_set_y`, `lv_obj_add_flag`, `lv_obj_clear_flag`, `LV_OBJ_FLAG_SCROLLABLE`, `LV_OBJ_FLAG_HIDDEN` | `board-verified` | `apps/smoke_assets` ran on board and returned `Lua app ok`. |
| Label long modes | `lv_label_set_long_mode`, `LV_LABEL_LONG_CLIP`, `LV_LABEL_LONG_WRAP`, `LV_LABEL_LONG_SCROLL_CIRCULAR` | `board-verified` | `apps/smoke_assets` ran on board and returned `Lua app ok`. |
| Asset paths and image object basics | `lv_resolve_asset_path`, `lv_asset_exists`, `lv_img_create`, `lv_img_set_src`, LVGL `S:` filesystem drive | `board-verified` | `apps/smoke_assets` verified `S:/apps/smoke_assets/assets/icon.bin`, `asset fs ok`, and `Lua app ok` on board. |
| BMP image decoder | `CONFIG_LV_USE_BMP=y`, `lv_extra_init`, BMP through `lv_img_set_src` | `board-verified` | `apps/smoke_visual` started on board, resolved `S:/apps/smoke_visual/assets/icon.bmp`, kept running without Lua/runtime errors, and the physical screen smoke showed the visual app image/progress UI. On 2026-06-25 the LVGL BMP decoder open-success check was fixed while diagnosing Weather backgrounds; `smoke_visual` still passed as the small-BMP control path. |
| Canvas BMP background loader | `lv_canvas_create`, `lv_canvas_load_bmp`, 320x240 RGB565 BMP assets, `lvgl_cfg.task_stack = 8192` | `board-verified metrics; physical visual pending` | Added after direct deferred `lv_img_set_src` for 320x240 PNG/BMP stayed at `background_error="loading"`. `lv_canvas_load_bmp` resolves app-local paths, decodes 16-bit 320x240 BMP rows into PSRAM scratch, then briefly locks LVGL to copy into the full-screen canvas buffer and invalidate. Serial diagnostics showed the default LVGL task stack overflowed during the deferred load, so the board config now sets the LVGL port task stack to 8192. Final Weather smoke on 2026-06-25 passed with `background_ready=true`, `background_attempts=1`, `background_error=""`, and clean stop to idle. Direct screen/photo QA is still needed for color, scale, and layout. |
| PNG image decoder | `CONFIG_LV_USE_PNG=y`, `lv_extra_init`, PNG through `lv_img_set_src` | `flashed; board visual pending` | `sdkconfig.defaults` previously enabled PNG, but the generated `sdkconfig` still disabled it. On 2026-06-24 both files were synchronized to `CONFIG_LV_USE_PNG=y`, static guardrails were added, `npm run test:firmware-static` passed, ESP-IDF build produced `vibeboard_runtime.bin` with 57% app partition free, and a later flash to `/dev/cu.usbmodem111301` verified bootloader/app/partition hashes. PNG rendering still needs board photo verification for resource apps and Weather. |
| GIF image widget | `CONFIG_LV_USE_GIF=y`, `lv_gif_create`, `lv_gif_set_src`, `voice_ai` `use_gif/gif_visible/gif_state/gif_src` metrics, `codex_buddy` `gif_visible/gif_state/gif_src` metrics | `board-verified metrics; physical visual pending` | Added for upstream `voice_ai` and `codex_buddy` buddy state animations. `lv_gif_set_src(obj, nil)` clears the current GIF and string paths resolve through the same app-local `S:` filesystem path bridge as `lv_img_set_src`. On 2026-06-23 `apps/voice_ai` was uploaded with `use_gif=true`, `npm run voice-ai:smoke -- --require-gif` required enabled visible GIF metrics before recording, and the board returned `gif=idle` while the mock bridge path completed. On 2026-06-25 the SD open-file budget was raised from 8 to 16 so `voice_ai` could keep GIF/font assets live while still serving app files and metrics; the flashed board then passed `voice-ai:smoke` with `gif=idle,font=loaded` and direct `/apps/file` reads while the app was running. Later the same day `codex_buddy` uploaded the upstream `assets/bufo` GIF pack and passed lifecycle smoke with `gif_visible=true,gif_state=attention,gif_src=S:/apps/codex_buddy/assets/bufo/attention.gif`; the cold-start bug from unguarded `lv_gif_create(root)` was fixed by wrapping GIF widget creation and keeping `draw_phase` metrics. This verifies app-selected GIF state and source metrics, not a camera/physical visual observation of the animation. |
| Shared CJK font coverage | `LV_FONT_COMMON_CN_13`, `LV_FONT_VOICE_AI_13`, `CONFIG_LV_FONT_SIMSUN_16_CJK=y`, `CONFIG_LV_FONT_DEFAULT_SIMSUN_16_CJK=y`, `LV_TXT_ENC_UTF8`, `apps/voice_ai/font/msyh_cn_3500_charset.txt` | `board-verified common font + flashed SimSun default` | The 2026-06-24 device photo showed small placeholder boxes consistent with missing glyphs because the firmware default was Montserrat 14. The build now enables SimSun CJK and makes it the LVGL default font in both `sdkconfig.defaults` and `sdkconfig`. On 2026-06-30 a shared 13 px common Chinese font, `LV_FONT_COMMON_CN_13`, was added to firmware from `msyh_cn_3500_charset.txt`; the charset covers the common 3500 Chinese characters, ASCII, common punctuation, `℃`, and `°`. Voice AI now prefers `LV_FONT_COMMON_CN_13`, falls back to `LV_FONT_VOICE_AI_13`, then `LV_FONT_SIMSUN_16_CJK`; board smoke returned `font=loaded`, `font_handle=-13014`, and `font_src=builtin:LV_FONT_COMMON_CN_13`. Future generated apps should use these shared fonts first and only add app-specific generated fonts for fixed-format special sizes such as large weather temperatures, game tiles, or dashboards. |
| Common widgets | `lv_btn_create`, `lv_bar_create`, `lv_bar_set_range`, `lv_bar_set_value`, `lv_slider_create`, `lv_slider_set_range`, `lv_slider_set_value`, `lv_slider_get_value`, `lv_list_create`, `lv_list_add_text`, `lv_list_add_btn`, `lv_switch_create`, `lv_dropdown_create`, `lv_dropdown_set_options`, `lv_dropdown_set_selected`, `lv_dropdown_get_selected`, `lv_textarea_create`, `lv_textarea_set_text`, `lv_textarea_add_text`, `lv_textarea_get_text`, `lv_roller_create`, `lv_roller_set_options`, `lv_roller_set_selected`, `lv_roller_get_selected`, `lv_arc_create`, `lv_arc_set_range`, `lv_arc_set_value`, `lv_arc_get_value`, `LV_ANIM_OFF`, `LV_ANIM_ON`, `LV_ROLLER_MODE_NORMAL` | `board-verified lifecycle; visual smoke pending for full controls slice` | `apps/smoke_visual` ran on board and logged timer-driven button/bar progress updates from 0 to 100 repeatedly. On 2026-06-22 `smoke_controls` was packaged, uploaded, confirmed in `/apps`, launched through `npm run lifecycle:smoke -- --app smoke_controls`, reached `state=running,current_app=smoke_controls`, then stopped cleanly. This proves the first common-control slice can start and keep the app alive on board; manual screen observation is still needed to verify every control renders correctly. |
| Phase 3 widget smoke app | `apps/smoke_lvgl_widgets`, `lv_label_create`, `lv_img_create`, `lv_img_set_src`, `lv_btn_create`, `lv_bar_create`, `lv_bar_set_value`, `lv_obj_add_flag`, `lv_label_set_long_mode` | `board-verified lifecycle; physical visual pending` | Added as the explicit Phase 3 widget sample required by `docs/development-plan.md`. The app is self-contained, uses an app-local BMP asset, defers its real UI work through a 50 ms boot timer, writes `metrics.json`, and writes `boot_error` when deferred boot fails instead of silently swallowing the error. A 2026-06-25 first board attempt exposed a transient SD apps/staging state: stale cached `/apps`, `/rescan` returning `ESP_ERR_NOT_FOUND`, `/apps/file?...main.lua` returning 404, and strict upload failing at `500 mkdir failed`. After `POST /stop` and `POST /reboot`, `/rescan` returned `{"ok":true,"app_count":45}`, `/apps/file` readbacks succeeded, and the stricter uploader confirmed the package. `npm run lifecycle:smoke -- --app smoke_lvgl_widgets --allow-starting --require-metrics ready=true --require-metrics boot_error= --require-metrics 'bar_value>=0' --stop` returned `metrics={"ready":true,"boot_error":"","icon_path":"S:/apps/smoke_lvgl_widgets/assets/icon.bmp","bar_value":20}` and stopped back to idle. Physical screen/photo verification of the rendered widget layout remains pending. |
| Phase 3 style smoke app | `apps/smoke_lvgl_style`, `lv_obj_set_style_opa`, `lv_obj_set_style_bg_opa`, `lv_obj_set_style_border_opa`, `lv_obj_set_style_text_font`, `lv_obj_set_style_text_align`, `lv_label_set_long_mode`, optional `lv_label_set_recolor` probe, `LV_PART_MAIN`, `LV_STATE_DEFAULT`, `LV_TEXT_ALIGN_CENTER` | `board-verified lifecycle; physical visual pending` | Added as the explicit Phase 3 style sample. It uses the existing selector/constants, defers real UI work through a 50 ms boot timer, writes `metrics.json`, writes `boot_error` on deferred boot failure, and probes `lv_label_set_recolor` via `rawget` so the app remains runnable before that optional binding is registered. Static tests assert the style API coverage. After the same board reboot/readback recovery described above, the stricter uploader confirmed the package and `npm run lifecycle:smoke -- --app smoke_lvgl_style --allow-starting --require-metrics ready=true --require-metrics boot_error= --require-metrics 'pulse>=0' --stop` returned `state=running,current_app=smoke_lvgl_style`, `metrics={"ready":true,"boot_error":"","pulse":0,"recolor_supported":false,"title_font":16,"metric_font":28,"badge_font":10}`, and stopped back to idle. Physical screen/photo verification of style rendering remains pending. |
| 2048 styles and object lifecycle | `lv_obj_del`, `lv_obj_move_foreground`, `lv_obj_set_style_bg_opa`, `lv_obj_set_style_bg_grad_color`, `lv_obj_set_style_bg_grad_dir`, `lv_obj_set_style_bg_main_stop`, `lv_obj_set_style_bg_grad_stop`, `lv_obj_set_style_border_opa`, `lv_obj_set_style_clip_corner`, `lv_obj_set_style_shadow_*`, `lv_obj_set_style_opa`, `LV_GRAD_DIR_VER` | `board-verified` | Upstream `2048` launched from SD and stayed `state=running,current_app=2048,last_status=ESP_OK` for about 90 seconds on 2026-06-19 after downgrading the game-over overlay away from unsupported small canvas blur, reusing freed LVGL object slots, clearing registered child handles during object deletion, and raising the Lua object handle table to 128. |
| Simple property animation | `lv_anim_start`, `lv_anim_del`, `lv_anim_delete`, `ANIM_X`, `ANIM_Y`, `ANIM_W`, `ANIM_H`, `ANIM_OPA`, `ANIM_PATH_EASE_OUT` | `board-verified` | Added for upstream `2048`; app launched and remained running on board, and user confirmed physical swipe gameplay. |

## Network, JSON, And Time

| Capability | APIs | Status | Evidence |
| --- | --- | --- | --- |
| WiFi STA | `wifi.mode`, `wifi.start`, `wifi.sta.config`, `wifi.sta.connect`, `wifi.sta.getip` | `board-verified` | `apps/smoke_network` connected to `1-306`, logged `sta got ip 192.168.1.32`, and Lua read the IP. |
| Runtime WiFi autoconnect | `vb_runtime_wifi_prepare`, `vb_runtime_wifi_start_from_sd`, `/sdcard/runtime/wifi.json`, compatibility read from `/sdcard/apps/smoke_network/wifi.json`, `POST /runtime/config?name=wifi` | `board-verified autoconnect + config writer + reboot wait; credential smoke tool-ready` | Boot logged `runtime WiFi autoconnect using /sdcard/runtime/wifi.json`, `wifi:config NVS flash: disabled`, `WiFi/LWIP prefer SPIRAM`, joined `1-306`, and logged `runtime sta got ip 192.168.1.32` before any app was manually launched on 2026-06-21. Runtime-owned `/sdcard/runtime/wifi.json` can be written through the install service and `npm run runtime:config -- <board-url> wifi <json-file|-|json>`. `POST /reboot` and `npm run runtime:config:smoke -- --reboot ...` are board-verified through an I2S config write. The smoke tool now supports `--expect-app-count <n>` and `--expect-ip <ip>` so a real WiFi credential write/reboot run can require the board to return as the expected Runtime instance instead of merely accepting any `/status` response. The final WiFi credential write-then-reboot smoke remains pending until a real local WiFi config is provided, because the repo only contains placeholder credentials. |
| HTTP client | `http.get`, `http.post`, `http.cubicserver.get` | `board-verified sync + callback/headers` | `http.get`/`http.post` synchronous forms are board-verified through `apps/smoke_network`; `http.get(url, opts, callback)` and `http.post(url, opts, raw_body, callback)` are board-verified through desktop-bridge apps. `apps/voice_ai` uses callback POST headers for raw PCM uploads. `apps/codex_buddy` uses `http.get(state_url(), { timeout_ms = 3000 }, cb)` against the local Codex Buddy bridge and `http.post(permission_url(), { headers = {...}, timeout_ms = 3000 }, raw, cb)` for permission decisions; board metrics showed `/state` returning 200 and HOME/SHORT producing `permission_posts=1,last_permission_decision="once",last_permission_status=200`. `http.cubicserver.get(path, opts, callback)` is exposed for migrated `weather` compatibility and maps relative paths through `/sdcard/runtime/cubicserver.json` `base_url`; missing/malformed config now fails fast instead of falling back to `http://cubicserver.local`, because that fallback could block app stop during DNS/HTTP resolution. `/runtime/config?name=cubicserver` writes the runtime-owned config and is also reachable through `npm run runtime:config`. On 2026-06-29 `tools/weather-bridge` was added as a local Open-Meteo to QWeather-shaped bridge: it defaults to Shanghai (`101020100`, `31.2304,121.4737`), exposes `/v1/weather/now`, and `apps/weather` now writes successful fetch values (`valid`, `temp`, `text`, `humidity`, `wind_speed`, `last_http_code`, `last_update_ms`) back to `metrics.json` for smoke verification. |
| JSON | `sjson.decode`, `sjson.encode`, `json.decode`, `json.encode` | `board-verified` | `sjson.*` is board-verified through `apps/smoke_network`; `json.*` is exposed as the same table for migrated `weather`/`voice_ai` compatibility, is covered by static tests plus ESP-IDF build, and is board-verified through the 2026-06-20 `weather` Cubicserver mock response decode path. |
| Time/NTP | `time.get`, `time.settimezone`, `time.initntp` | `board-verified` | `apps/smoke_network` ran no-credentials and WiFi-credentials paths without `Invalid mbox` and logged `time now ...`. |

On 2026-06-25 the Voice AI board smoke also recovered from an SD open-file budget issue by raising `VB_SD_MAX_OPEN_FILES` to 16. After flashing the board, `voice_ai` could serve `metrics.json`, `main.lua`, and `app.info` while running, and `npm run voice-ai:smoke` returned `voice-ai smoke ok: audio=1536 chats=0->1 state=running current_app=voice_ai gif=idle font=loaded reply=reply:transcript:2048`. That run verified the board-side command-provider transcript/reply round-trip on the flashed device; the remaining gap is a production STT/provider path plus physical GIF visual QA.

## Audio And Voice AI

| Capability | APIs | Status | Evidence |
| --- | --- | --- | --- |
| I2S RX/TX audio path | `i2s.start`, `i2s.read`, `i2s.write`, `i2s.stop`, `i2s.status`, `i2s.record_file`, `i2s.play_file`, `vb_i2s_tx_stream_begin/write/end`, `/sdcard/runtime/i2s.json`, `POST /runtime/config?name=i2s`, `GET /apps/file?app=smoke_i2s&path=metrics.json`, `npm run i2s:smoke -- --require-tone-writes <n>`, ES8311, ES7210, PCA9557 PA_EN | `board-verified RX + TX + speaker + file-backed voice capture/playback` | Runtime registers a Lua `i2s` module for Voice AI recording and audio playback experiments. The module supports ESP-IDF standard-mode RX/TX plus file-backed capture/playback helpers, reads `bclk_pin`, `ws_pin`, `din_pin`, `dout_pin`, and `mclk_pin` from `/sdcard/runtime/i2s.json`, exposes `rx_started`, `tx_started`, `reads`, `rx_bytes`, `writes`, `tx_bytes`, and pins through `i2s.status`, and recreates the channel when a later `i2s.start(...)` requests a different RX/TX mode instead of returning stale `started=true`. On 2026-06-30 the LCKFB official audio wiring was written to the board with MCLK 38, BCLK 14, WS 13, DIN 12, DOUT 45. The Lua direct write regression was fixed so `i2s.write()` uses `i2s_channel_write()` directly instead of a non-in-use TX stream; after flashing, `npm run i2s:smoke -- --require-write --require-tone-writes 8` returned `i2s smoke ok: ... writes=10 tx_bytes=2560 tone_writes=10 error=`. Board audio preparation initializes ES8311 for TX, re-prepares ES7210 for RX before captures to avoid stale all-zero recordings after app switches, preserves PCA9557 output bits, and enables PCA9557 PA_EN for speaker output. `i2s.play_file` now uses 8-bit-capable heap allocation instead of internal-only buffers, fixing post-recording `i2s play_file buffer alloc failed` under memory pressure. The user confirmed the 440 Hz tone was audible on the physical speaker on 2026-06-30. On 2026-07-01, after flashing `/dev/cu.usbmodem112301`, `audio_loopback` completed file-backed capture/playback with `mode=idle`, `record_bytes=384000`, `play_bytes=128968`, and `last_error=""`. Earlier board evidence also covers nonzero RX, sustained TX tone metrics, Spectrum real-I2S analyzer metrics, and NES host-audio backend selection. Remaining risk is long-duration audio soak and production-grade gain/noise tuning, not basic codec/amp bring-up. |
| Voice AI desktop bridge | `POST /api/chat`, `GET /api/result`, `GET /health`, `GET /debug/stats`, `node desktop-bridge/server.mjs --provider mock|command`, `npm run voice:openai:transcribe`, `npm run voice:openai:reply`, `VOICE_BRIDGE_REPLY_INCLUDE_AUDIO`, `--once-file <pcm>`, `npm run voice-ai:smoke` | `board-verified mock + command-provider + local Apple Speech physical STT/reply` | Provider-neutral desktop bridge accepts raw PCM uploads from `apps/voice_ai`; the app now uses the packaged `voice_audio` helper for `i2s.record_file` capture, 48 kHz stereo/TDM to 16 kHz mono conversion, and `/api/chat` upload headers. The bridge supports synchronous and pending-job responses, exposes `/debug/stats` for board smoke verification, and returns `transcript`, `reply`, and optional `ui_code`. The default provider is a deterministic mock. The `command` provider runs local STT/reply commands through JSON stdin/stdout so credentials and cloud SDK choices stay outside the device protocol, and `/health` plus `/debug/stats` expose the active provider name. `--once-file` can run a local PCM sample through the same provider pipeline and optionally write the JSON result. The command-provider privacy boundary is test-covered: transcribe commands receive `audio_base64`, while reply commands receive only `transcript` and `metadata` unless `VOICE_BRIDGE_REPLY_INCLUDE_AUDIO=1` is set. On 2026-06-24 an OpenAI-compatible command wrapper was added: `voice:openai:transcribe` converts PCM to WAV and calls `/v1/audio/transcriptions`, while `voice:openai:reply` calls `/v1/responses` with transcript-only input by default or `/v1/chat/completions` when `OPENAI_REPLY_ENDPOINT=chat_completions` is set. Mocked HTTP tests cover both wrappers. On 2026-06-25 a real local one-shot used present `OPENAI_API_KEY`/`OPENAI_BASE_URL` and a generated 16 kHz PCM sample; `/models` worked, but the configured compatible service returned `HTTP 404 text/plain: 404 page not found` for `/audio/transcriptions`, and a direct reply-command probe returned `HTTP 502 text/html; charset=UTF-8` for `/responses`. A follow-up reply-only probe with `OPENAI_REPLY_ENDPOINT=chat_completions` and `OPENAI_REPLY_MODEL=gpt-5.4-mini` returned `{"reply":"VibeBoard 语音回复端点可用。","ui_code":""}`. On 2026-07-01, `voice_ai` was moved to the native file-backed `i2s.record_file` lane, uploaded 98304 bytes of `pcm_s16le/16000/16/1` to the command provider, and the user physically confirmed that the board recognized speech and displayed the returned text. Bridge stats for that successful local Apple Speech path showed `chat_requests=8`, `last_audio_bytes=98304`, `last_transcript` populated, `last_reply="识别结果：..."`, `duration_seconds=3.072`, `peak_abs=5462`, `rms_abs=420.221`, and `silent_ratio=0.621745`. This proves the local device-to-desktop STT/reply/display loop. Cloud STT/LLM providers still need separate credential, endpoint, privacy, and logging verification. |

## Device Install Service

| Capability | APIs | Status | Evidence |
| --- | --- | --- | --- |
| SD app file upload / staged commit | `POST /install?app=<id>&path=<relative>`, `POST /install/commit?app=<id>&stage=<stage>` | `board-verified upload + staged replacement; rollback failure restore static-verified` | Runtime boot logged `install_service: install service listening on port 8080`; raw HTTP POST returned `200 OK` and `ok`; 2026-06-19 uploader check uploaded `2048` through staged install and commit. On 2026-06-25 `/install/commit` was hardened so a replacement commit no longer deletes the existing app before moving the staged app into place: it moves the previous app to `/sdcard/.vibeboard-staging/<stage>.rollback`, moves the staged app to the final path, restores the rollback directory on replacement failure, and cleans rollback after success. The regression was verified red-green in `tools/firmware-static-check`, and `npm run test:firmware-static` passed 103/103; ESP-IDF build passed with `vibeboard_runtime.bin` size `0x1b81e0` and 57% app partition free. After flashing `/dev/cu.usbmodem111301`, the board returned `sd=true,app_count=45,state=idle`; a temporary `rollback_smoke` v1 staged upload committed and read back as v1, then v2 replaced the same app id and `/apps/file` read back v2 `app.info` plus `main.lua`. Cleanup deleted the temp app, restored `app_count=45`, and readback returned 404. This verifies the normal replacement path on board; forced rename failure or power-loss restore still needs dedicated fault-injection evidence. |
| Device status | `GET /status` | `board-verified` | 2026-06-21 raw HTTP GET returned valid JSON with `sd=true`, `app_count=24`, `runtime_version=0.1.0`, `package_schema=vibeboard-runtime-app-package@2`, and `native_abi_version="vibeboard-native-module-abi@1"`; `npm run device:check` recognized the board as VibeBoard Runtime from this metadata. |
| App lifecycle status | `GET /status` field `state` with `idle`, `starting`, `running`, `stopping`, `failed`; compatibility fields `running` and `current_app` remain | `board-verified` | Board HTTP checks verified idle, running with `current_app=smoke_visual_remote`, controlled stop back to idle, intentional Lua failure as `failed`, and recovery from failed through `smoke_network`. |
| App listing | `GET /apps` | `board-verified` | 2026-06-19 board check returned chunked JSON with 22 apps after raising registry capacity to 32 and moving large registry/rendered-app buffers to PSRAM. Compatible schema v2 apps are `2048`, `matrix_rain`, `nixie_clock`, `clock`, `conway_life`, and `fluid_pendant`; legacy schema v1 apps remain visible through HTTP as `compatible:false` but are hidden from Launcher. On 2026-06-22 the scan path was hardened after a board run showed `app_count=0` and upload commit failure: `vb_app_registry_scan()` now builds a PSRAM/heap temporary result and only replaces the cached registry after a successful scan, so transient `opendir("/sdcard/apps")` failure no longer clears `/apps`. The repaired firmware was flashed and verified with `/status`, `/apps`, `/rescan`, and stop-after-app checks all preserving `app_count=31`. |
| Incompatible app guard | manifest `compatible`, Launcher filtering, `/launch` rejection | `board-verified` | Old schema v1 apps remain visible in `/apps` as `compatible:false`, but Launcher skips them and HTTP `/launch` returns `400 app incompatible`; compatible `2048` still launches. |
| App rescan | `POST /rescan` | `board-verified` | Raw HTTP POST originally returned `{"ok":true,"app_count":2}` after re-running `vb_app_registry_scan`. On 2026-06-22 repaired firmware returned `{"ok":true,"app_count":31}` after the cached-registry hardening, and stop-after-app `/rescan` also stayed at 31. |
| Mac uploader tool | `npm run upload:app -- http://<ip>:8080 dist/apps/<app-id> <app-id>` | `board-verified; stricter readback check build-verified` | Default Node native HTTP uploaded `smoke_visual_native`, `2048`, `matrix_rain`, `nixie_clock`, `clock`, `conway_life`, and `fluid_pendant` to the board, uploaded files, called commit/rescan as needed, and confirmed apps through `/apps` without `--transport nc`. On 2026-06-25 the uploader was tightened to read back `/apps/file?app=<id>&path=app.info` after `/apps` confirmation, because a stale cached registry can list an app while `/sdcard/apps` is not readable. `npm run test:uploader` passed 42/42. The current board correctly fails the stricter upload with `Upload app.info failed after 3 attempts: 500 mkdir failed`, exposing a board SD/staging write problem instead of a false confirmation. |
| Interrupted staged upload smoke | `npm run upload:interrupted-smoke -- http://<ip>:8080 interrupted_upload_smoke` | `board-verified` | The tool creates a temporary staged app package, intentionally fails one file upload, verifies the uploader calls `/install/abort`, and confirms the temporary app is absent from `/apps`. On 2026-06-20 the board remained `state=idle,running=false` with 24 apps after the smoke. |
| App launch | `POST /launch?app=<id>` | `board-verified; stale-cache hardening build-verified` | Raw HTTP POST returned `{"ok":true,"launched":"smoke_visual_remote"}`; serial logs showed `Lua async launch: smoke_visual`, app-local SD asset resolution, and repeated progress updates. 2026-06-19 HTTP launch/status checks also verified `nixie_clock`, `matrix_rain`, `clock`, `conway_life`, and `fluid_pendant` as `state=running,current_app=<id>,last_status=ESP_OK`. On 2026-06-22 `/launch` was changed to queue a deferred launch task and return before Lua script preload/app task creation can block the HTTP response; after flashing `/dev/cu.usbmodem111301`, the previous `smoke_gamepad` metrics launch timeout was gone and `gamepad smoke ok` completed with `connected=true,updates=5,xbox=true`. On 2026-06-23 a larger `voice_ai` upload exposed `500 ESP_ERR_NO_MEM` in deferred launch queueing; the job was already heap-caps allocated but the deferred launch task still used an internal-RAM stack. The task now uses `xTaskCreatePinnedToCoreWithCaps(..., MALLOC_CAP_SPIRAM | MALLOC_CAP_8BIT)` and frees the job with `heap_caps_free`; after build/flash, `voice-ai:smoke --require-gif` launched successfully. On 2026-06-25 source hardening changed `/launch` to reject any `vb_app_registry_scan()` failure instead of continuing on `ESP_ERR_NOT_FOUND`, so a stale cached `/apps` entry cannot be launched when `/sdcard/apps` cannot be opened. This hardening is covered by `npm run test:firmware-static` and still needs a firmware flash before it becomes board evidence. |
| Mac launch tool | `npm run launch:app -- http://<ip>:8080 <app-id>` | `board-verified` | Default Node native HTTP launched `smoke_visual_native`, `smoke_fail`, and `smoke_network` without `--transport nc`; the launch check exposed and then verified the HTTPD 4096-byte stack overflow fix by increasing the install service HTTPD stack to 8192 bytes. |
| Local device web UI | `npm run device:web -- --board http://<ip>:8080 --port 8790`; `npm run device:web:smoke -- --board http://<ip>:8080 --base http://127.0.0.1:8790` | `board-verified` | `tools/device-web-ui/server.mjs` serves a local browser UI and a narrow proxy for `/status`, `/apps`, `/launch`, `/stop`, `/rescan`, and `/apps/delete` so the board firmware does not need CORS headers. On 2026-06-20 `http://127.0.0.1:8790/api/status` showed `state=running,current_app=weather,last_status=ESP_OK`, `/api/apps` returned 24 apps, and the HTML page returned `200 OK`. Follow-up smoke uploaded temporary `web_ui_smoke`, exercised launch/stop/rescan/delete through the Web UI proxy, then confirmed `/apps` had 24 apps and `web_ui_smoke` was absent. This local browser UI is separate from the Runtime-side WebUI route bridge below. |
| App stop | `POST /stop`, `vb_app_runner_stop`, `vb_app_runner_wait_stopped` | `board-verified` | Switching away from `smoke_visual_remote` logged `Lua stop requested`, `Lua tmr loop stop requested`, and `message=stopped`; idle `POST /stop` returned `{"ok":true,"stopped":false}`. `VB_APP_RUNNER_STOP_TIMEOUT_MS` is now 5000 ms after `conway_life` exposed that the old 1500 ms window could report timeout even though the app stopped cleanly shortly after. |
| Active app key injection smoke hook | `POST /input/key?code=<name>&event=<name>`, `npm run input:smoke -- --app <id> --key CODE:EVENT ...` | `board-verified endpoint + test-covered tool` | Added as a narrow automation hook for hardware smoke tests. The handler accepts only named key codes/events and calls `vb_app_runner_enqueue_key(code, event)`; it does not call Lua directly. On 2026-06-22 the reflashed board returned `400 no active app` while idle, returned `{"ok":true,"injected":...}` for `smoke_key` LEFT/HOME injections, and `HOME` returned `smoke_key` to idle through the normal default HOME stop path. The local `input:smoke` wrapper launches an app, injects a key sequence, and checks final `/status`, so future input/Voice AI smoke runs do not need hand-written curl sequences. On 2026-06-25 `codex_buddy` proved the opposite mode too: an app can call `app.set_home_exit(false)`, receive HOME itself through a per-key `key.on(code, cb)` wrapper, post a permission decision, then restore default HOME/EXIT handling during stop; board metrics after `POST /input/key?code=HOME&event=SHORT` showed `last_key="HOME",permission_posts=1,last_permission_decision="once",last_permission_status=200`. |
| App switch | `POST /launch?app=<new-id>` while another app is running | `board-verified` | `POST /launch?app=smoke_network` while `smoke_visual_remote` was running returned `200 OK`; serial logs showed visual stopped, then `smoke_network` launched and completed with `ESP_OK`. 2026-06-19 regression verified `conway_life -> fluid_pendant` succeeds after increasing the shared stop wait to 5000 ms. |
| Runtime WebUI route bridge | `app.route_base()`, `app.set_webui(bool)`, `app.route(path, callback)`, `/app/*` -> active Lua app callback | `board-verified` | Active-app route bridge is wired through `lua_app.c/h`, `app_runner.c/h`, and `install_service.c`. `smoke_app_manager` declares `capabilities = lvgl,timer,input,file,webui`, registers `/api/ping`, writes `metrics.json`, and the board returned `GET /app/api/ping -> {"ok":true,"route":"ping","app":"smoke_app_manager"}` with metrics `webui_enabled=true,webui_registered=true,webui_requests=1,last_webui_path="/api/ping",route_base="/app"`. `mini_claw` was re-smoked after fixing its timer mode to `tmr.ALARM_AUTO`; board GET `/app/api` returned `{"ok":true,"route":"api","method":"GET"}`, POST brightness `{"action":"brightness","level":30}` returned `{"ok":true,"level":30}`, and `/stop` returned the board to idle. `npm run app-manager:smoke` still handed off to `smoke_key`. This bridge now passes request method/body through to Lua, but it still does not cover static assets, auth, CORS, or full `mini_claw`. |
| Device Launcher | native LVGL app list, tap-to-launch, BOOT short-select/long-launch via `vb_launcher_ui_show` | `board-verified` | Board booted `vibeboard_runtime` from `factory 0x10000`, skipped missing `raw_upload/main.lua`, reported `VibeBoard Runtime ready: sd=ok apps=2 launcher=ok`; touch tap-to-launch works on the device screen, and BOOT short/long press provides a hardware fallback. |
| Phase 5B launcher lifecycle controls | native launcher stop control, refresh/rescan control, return-to-launcher after stop or async failure, BOOT long-press stop while launcher is inactive, screen failure feedback from `vb_app_runner_last_message` | `board-verified` | HTTP and serial board checks verified stop, rescan, return to idle after stop, failed state from `smoke_fail`, and recovery through `smoke_network`. Manual physical screen smoke confirmed native Stop, Refresh, return-to-launcher, readable failure feedback, and BOOT long-press stop. |

## Planned Runtime Modules

| Capability | Target APIs | Status | Notes |
| --- | --- | --- | --- |
| Lua App manager | `app.list`, `app.rescan`, `app.current`, `app.exiting`, `app.exit([reason])`, `app.launch(id)`, `app.set_home_exit(enabled)`, `app.on("exit"|"launch"|"stop", cb)`, `npm run app-manager:smoke` | `board-verified app manager + automated handoff smoke; build-verified physical BOOT HOME` | Runtime registers a Lua `app` module in each app runner. `app.list()` and `app.rescan()` now read the cached registry snapshot handed to the running Lua app instead of rescanning SD while WiFi/LVGL/Lua are active. `app.current()` exposes current runner state and last result, `app.exiting()` mirrors the runner stop flag, `app.exit([reason])` dispatches a `stop` event and requests the active runner to stop from inside Lua, `app.launch(id)` dispatches a `launch` event, queues a non-reentrant app-to-app handoff, and lets the runner start the next app only after the current Lua state has cleaned up. `apps/smoke_app_manager` was board-verified on 2026-06-21: it saw `count: 25`, `rescan: 25`, handled a software HOME short event through `key.push(key.HOME, key.SHORT)`, returned `app.launch("smoke_key") true`, handed off to `smoke_key`, kept `/status` responsive with `current_app=smoke_key,last_status=ESP_OK`, and stopped cleanly through `/stop`. On 2026-06-22 `npm run app-manager:smoke -- --board http://192.168.1.32:8080 --polls 12 --interval-ms 500` repeated the handoff automatically and returned `app-manager smoke ok: smoke_app_manager->smoke_key state=running current_app=smoke_key polls=5`; `/stop` then returned `{"ok":true,"stopped":true}`. Physical BOOT short/long press forwarding into Lua HOME is still pending manual button smoke. |
| Input | `key.on`, `key.repeat_start`, `touch.on`, `gamepad.state`, `gamepad.start`, `gamepad.stop`, `gamepad.on`, `gamepad.off`, `gamepad.rescan`, `gamepad.push_state`, `npm run gamepad:smoke` | `board-verified touch + HTTP key + gamepad software lifecycle metrics + NES native host synthetic gamepad; build-verified BOOT HOME/repeat` | First touch-swipe-to-`key.on` bridge is board-verified through `2048`. Runtime now registers Lua `touch` and `gamepad` compatibility modules so migrated UI/NES code can read touch coordinates, start/read gamepad state, subscribe to events, and drive input from stable state shapes. The active app runner exposes a narrow `vb_app_runner_enqueue_key(...)` bridge so Launcher-owned physical BOOT input can be forwarded as Lua `key.HOME` events after the Launcher has handed off the screen. The key module also provides timer-backed repeat helpers that generate LONG_START/LONG_REPEAT/LONG_END through the queued Lua event path, which reduces duplicated repeat counters in interactive apps while real hardware repeat sources are still being proven. The `gamepad` compatibility layer now dispatches `EVT_CONNECTING` from `start/rescan`, `EVT_CONNECTED`/`EVT_DISCONNECTED` on state transitions, `EVT_CONNECT_FAILED` when a pending connection is cleared without a connection, and `EVT_UPDATE` for every pushed state. On 2026-06-22 `smoke_gamepad` was upgraded to write `metrics.json` with `connected`, `updates`, `xbox_seen`, dpad seen flags, address, and event counters. After the registry and deferred-launch fixes were flashed, `npm run gamepad:smoke -- --board http://192.168.1.32:8080 --polls 8 --metrics-delay-ms 3000 --metrics-polls 16 --interval-ms 500 --require-updates 2 --require-connected --require-xbox` returned `gamepad smoke ok: smoke_gamepad state=running current_app=smoke_gamepad polls=1 connected=true updates=5 xbox=true buttons=256`, and `/stop` returned to idle with `app_count=31`. On 2026-06-24 the smoke was extended with `connect_failed` metrics plus `--inject-disconnect`, `--require-disconnects`, and `--require-connect-failed`; the board passed `npm run gamepad:smoke -- --inject-gamepad --inject-disconnect --require-updates 2 --require-disconnects 1 ...` with `updates=8,disconnects=1,connect_failed=0`. This board-verifies the software `gamepad.push_state` event, connected/update/disconnected lifecycle metrics, and HTTP runner queue path. On 2026-06-24, `npm run nes:smoke -- --require-host-gamepad-mask --inject-gamepad --inject-buttons 1 --inject-dpad-right` returned `host_gamepad=129`, and `smoke_nes/metrics.json` reported `host_gamepad_active=true,host_gamepad_mask=129`, proving synthetic HTTP/Lua gamepad input reaches the native NES host API. Real BLE/Xbox discovery, physical BOOT forwarding smoke, hardware-origin long press repeat, and physical controller input remain pending. |
| Native modules | `require("nes")` through the runtime native module searcher, `native_abi_version = vibeboard-native-module-abi@1`, `npm run nes:rom:smoke`, `npm run nes:smoke`, `npm run nesgame:smoke`, `GET /apps/file?app=<id>&path=metrics.json` | `board-verified legal ROM startup + cleanup + Lua PCM fallback + host I2S backend + synthetic native gamepad host; current 1600-frame host-audio baseline needs re-diagnosis` | Runtime now exposes a native ABI version, installs a Lua native module searcher before app entry execution, validates app-local `native/nes.vbn` descriptors, routes validated modules through a static adapter facade, initializes host API groups, calls the linked NES adapter entrypoint through the `vb_native_module_init(&host_api, &module)` contract, returns the native module handle in `vb_native_module_result_t.module`, copies the host API table into the linked adapter instance for safe callback lifetime, binds Lua `nes.state/start/stop/read_audio/input` closures over that native module handle, validates the ROM iNES header through host file APIs, builds a `module_host_api_v1` shim over the current Runtime host API, links the upstream NES C++ core into the firmware, maps task create/remove, and now converts byte-sized task stack requests into FreeRTOS stack words before `xTaskCreatePinnedToCore`, parses `nes.start(path, opts)` display/task/audio options plus migrated `nesgame` aliases such as `frame_width`, `frame_height`, `center_on_screen`, and `audio_ringbuf_samples`, creates a `nes_core_bridge` runtime during native-module init, starts/stops it through `nes_core_start(...)`/`nes_core_stop(...)`, maps Runtime button masks to NES pad masks, bridges TFT-style `setAddrWindow/pushPixelsDMA` to the Runtime rectangle DMA path, routes RGB565 writes to the board LCD through a native display takeover that pauses LVGL while NES owns the panel, exposes core status fields and `rendered_frames` aliases through `nes.state()`, and exposes queued PCM bytes through `nes.read_audio([max_bytes])`. `apps/smoke_nes` isolates this loader path from the full NES game UI, covers `nes.input.set_mask`, writes periodic `metrics.json`, uses app-local ROM path `/sdcard/apps/smoke_nes/roms/smoke.nes`, and reports `rom_present`, `started`, `running`, `rendered_frames`, `audio_requested`, `audio_backend`, `audio_error`, `audio_bytes`, and `last_error`. `apps/nesgame` now also writes selector/running metrics, scans app-local `roms/`, maps those relative entries back to `/sdcard/apps/nesgame/roms/<file>` for `nes.start`, and has `npm run nesgame:smoke` to upload the legal smoke ROM, launch the selector, inject `UP/LONG_START`, and require frame growth. On 2026-06-23 the user-observed restart loop was traced to `task_wdt` starvation in `nes_core` / `nes_apu`; `nes_port_yield()` now feeds through the host task yield path and is called from both the main frame loop and APU batch loop. After flashing the fix to `/dev/cu.usbmodem111301`, `npm run nes:rom:smoke -- --board http://192.168.1.32:8080 --app smoke_nes --path roms/smoke.nes` uploaded the generated legal mapper-0 iNES ROM, and `npm run nes:smoke -- --board http://192.168.1.32:8080 --polls 8 --metrics-polls 120 --interval-ms 500 --require-rom --require-frame-growth 1800 --require-audio-bytes 128` returned `nes smoke ok: smoke_nes state=running current_app=smoke_nes polls=2 rom=true started=true frames=1816 frame_growth=1804 samples=118 frames=12->1816 audio=1024`; follow-up `nesgame` smoke reached `frames=433->722` manually and `npm run nesgame:smoke -- --board http://192.168.1.32:8080 --require-frame-growth 120 --metrics-polls 30 --selector-polls 12 --interval-ms 500` returned `nesgame smoke ok: ... frames=12->139 frame_growth=127 audio=0 backend=none`. The host audio shim now routes `module_audio_api_t.begin/write/end` to the shared I2S TX helper. Its first board smoke exposed `audio_error="create nes apu task failed"`; after the shim started only `nes_apu` with a SPIRAM-capable task stack and deleted it with `vTaskDeleteWithCaps`, `npm run nes:smoke -- --require-audio-backend host` returned `frames=11->137 frame_growth=126 backend=host audio_error=`. On 2026-06-24, `cleanup_lua_runtime()` gained a native cleanup hook, and `vb_nes_native_module_cleanup()` now force-stops and destroys the static NES runtime when a Lua app exits. After flashing that firmware, `smoke_nes` host-audio smoke passed, `/stop` returned `{"ok":true,"stopped":true}`, and a no-reboot `smoke_i2s` launch reached `tone_writes=26,tx_bytes=6656` without serial `task_wdt CPU 0: nes_core` logs. A later 2026-06-24 board smoke proved the native gamepad host path: `npm run nes:smoke -- --require-host-gamepad-mask --inject-gamepad --inject-buttons 1 --inject-dpad-right` returned `host_gamepad=129`, and `metrics.json` reported `host_gamepad_active=true,host_gamepad_mask=129`. On 2026-06-25 a later recheck after reflashing the staged rollback firmware found the latest host-audio frame-growth baseline no longer reaches the old `1600` threshold: after re-uploading current `smoke_nes` and the legal ROM, the same `--metrics-polls 140 --require-frame-growth 1600 --require-audio-bytes 128 --require-audio-backend host` run reached ROM/host-audio metrics but only `frame_growth=1394` with `audio_backend="host"` and `audio_error=""`; a lower-threshold retry hit a transient `ECONNRESET` while reading `metrics.json`, and the board recovered to idle. This still verifies legal-ROM startup, host-audio backend selection, cleanup, and synthetic host gamepad paths, but the latest long frame-growth/metrics-read reliability needs re-diagnosis before claiming sustained NES performance. Hardware speaker/codec output, real BLE/Xbox input, physical UX observation, and longer real-game performance soak remain pending. |
